#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@ryansonshine/commitizen/bin/git-cz" "$@"
  ret=$?
else 
  node  "$basedir/../@ryansonshine/commitizen/bin/git-cz" "$@"
  ret=$?
fi
exit $ret
